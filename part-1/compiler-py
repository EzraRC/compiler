from lexer import *

def main():
    source = "test IF + - 1 NO 482 283 1 / 3 1 + next"
    lexer = Lexer(source)

    token = lexer.getToken()
    while token.kind != TokenType.EOF:
        print(token.kind)
        token = lexer.getToken()

main()